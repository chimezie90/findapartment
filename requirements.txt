# Core dependencies
pyyaml>=6.0
python-dotenv>=1.0
pycraigslist>=1.0.1
requests>=2.31
jinja2>=3.1

# Optional: SendGrid for email (alternative to SMTP)
# sendgrid>=6.10

# Development dependencies
pytest>=7.4
pytest-cov>=4.1
black>=23.0
ruff>=0.1
